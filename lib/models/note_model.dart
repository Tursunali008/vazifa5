class NoteModel {
  String id;
  String title;
  String content;
  DateTime createdDate;

  NoteModel({
    required this.id,
    required this.title,
    required this.content,
    required this.createdDate,
  });
}
